version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - dupl
    - errorlint
    - gosec
    - misspell
    - revive
  settings:
    dupl:
      threshold: 70

formatters:
  enable:
    - gofmt
